[tool.poetry]
name = "git-cheater"
version = "0.0.1"
description = "git-cheater"
authors = ["HE Yaowen <he.yaowen@hotmail.com>"]
license = "GPL-3.0"
packages = [{include = "git_cheater", from = "src"}]

[tool.poetry.dependencies]
python = "^3.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
