[tool.poetry]
name = "stbl"
version = "0.0.1"
description = "stbl"
authors = ["HE Yaowen <he.yaowen@hotmail.com>"]
license = "GPL-3.0"
packages = [{include = "stbl", from = "src"}]

[tool.poetry.dependencies]
python = "^3.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
