from setuptools import setup

setup(name='sol_stats',
      version='0.1',
      description='this is a package created by vinit to find stats from a list',
      packages=['sol_stats'],
      zip_safe=False)