from setuptools import setup

setup(name='declensions',
      version='0.1',
      description='Declension of words from the languages depending on the count.',
      packages=['declensions'],
      author_email='beeeirym@gmail.com',
      zip_safe=False)