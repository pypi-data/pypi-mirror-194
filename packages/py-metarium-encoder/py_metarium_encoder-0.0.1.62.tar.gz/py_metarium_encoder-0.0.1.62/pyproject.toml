[project]
name = "py_metarium_encoder"
version = "0.0.1.62"
authors = [
  { name="MetariumProject" },
]
description = "Python package for Metarium Encoder"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dynamic = ["dependencies"]
[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}
[project.urls]
"Homepage" = "https://github.com/MetariumProject/py-metarium-encoder"
"Bug Tracker" = "https://github.com/MetariumProject/py-metarium-encoder/issues"