from setuptools import setup

setup(name='compounding_interest',
      version='0.1',
      description='For calculating compound interest',
      author='B A L A N',
      packages=['compounding_interest'],
      zip_safe=False
      )